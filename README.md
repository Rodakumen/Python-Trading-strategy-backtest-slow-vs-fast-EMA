# Python-Trading-strategy-backtest-slow-vs-fast-EMA
## Moving Average Cross Strategy Backtesting Framework  This project implements and evaluates a configurable moving average crossover trading strategy using the `backtesting.py` framework, with optional filters (ADX, trend, volatility regime) to reduce whipsaws and improve risk-adjusted returns.
